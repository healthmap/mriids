import styled from "styled-components";

export const Title = styled.h2`
  font-size: 3.2rem;
  font-weight: 700;
  text-align: center;
  margin: 4.8rem 0;
`;

export const Body = styled.p`
  font-size: 1.6rem;
  font-weight: 400;
  line-height: 1.75;
  margin: 2.4rem auto;
  max-width: 70rem;
`;
